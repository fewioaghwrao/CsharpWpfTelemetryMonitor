<Window x:Class="WpfApp.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="設定"
        Height="620"
        Width="520"
        MinHeight="620"
        MinWidth="480"
        Background="#F3F4F6"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="24"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Width" Value="140"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#374151"/>
        </Style>

        <Style x:Key="TextInputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ComboInputStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Width" Value="240"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="96"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.92"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="96"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F9FAFB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SubActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,16,0,0"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Border Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="18"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- タイトル -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="設定"
                               FontSize="22"
                               FontWeight="SemiBold"
                               Foreground="#111827"/>
                    <TextBlock Text="しきい値、計測周期、音声読み上げ設定を変更できます。"
                               Margin="0,6,0,0"
                               FontSize="13"
                               Foreground="#6B7280"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <!-- 基本設定 -->
                <StackPanel Grid.Row="2">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="警告閾値"
                                   Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox Width="100"
                                 Style="{StaticResource TextInputStyle}"
                                 Text="{Binding AlertThreshold, UpdateSourceTrigger=PropertyChanged}"
                                 MaxLength="2"
                                 InputMethod.IsInputMethodEnabled="False"
                                 PreviewTextInput="AlertThresholdTextBox_PreviewTextInput"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="サンプリング周期 (ms)"
                                   Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox Width="100"
                                 Style="{StaticResource TextInputStyle}"
                                 Text="{Binding SampleIntervalMs, UpdateSourceTrigger=PropertyChanged}"
                                 PreviewTextInput="AlertThresholdTextBox_PreviewTextInput"
                                 MaxLength="4"
                                 InputMethod.IsInputMethodEnabled="False"/>
                    </StackPanel>
                </StackPanel>

                <!-- 音声設定 -->
                <GroupBox Grid.Row="3" Header="音声設定">
                    <StackPanel Margin="4">
                        <CheckBox Content="音声を有効にする"
                                  IsChecked="{Binding IsVoiceEnabled, Mode=TwoWay}"
                                  FontSize="14"
                                  Foreground="#374151"
                                  Margin="0,0,0,12"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="音声"
               Grid.Column="0"
               VerticalAlignment="Center"
               Foreground="#374151"
               FontSize="14"/>

                            <ComboBox Grid.Column="1"
              Margin="12,0,12,0"
              Height="36"
              ItemsSource="{Binding InstalledVoices}"
              SelectedItem="{Binding SelectedVoice, Mode=TwoWay}" />

                            <!-- 3列目は空でそろえる -->
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="速度"
               Grid.Column="0"
               VerticalAlignment="Center"
               Foreground="#374151"
               FontSize="14"/>

                            <Slider Grid.Column="1"
            Margin="12,0,12,0"
            Minimum="-10"
            Maximum="10"
            TickFrequency="1"
            IsSnapToTickEnabled="True"
            Value="{Binding VoiceRate, Mode=TwoWay}" />

                            <TextBlock Grid.Column="2"
               Text="{Binding VoiceRate}"
               VerticalAlignment="Center"
               HorizontalAlignment="Right"
               TextAlignment="Right"
               Foreground="#374151"
               FontSize="14"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="音量"
               Grid.Column="0"
               VerticalAlignment="Center"
               Foreground="#374151"
               FontSize="14"/>

                            <Slider Grid.Column="1"
            Margin="12,0,12,0"
            Minimum="0"
            Maximum="100"
            TickFrequency="1"
            IsSnapToTickEnabled="True"
            Value="{Binding VoiceVolume, Mode=TwoWay}" />

                            <TextBlock Grid.Column="2"
               Text="{Binding VoiceVolume}"
               VerticalAlignment="Center"
               HorizontalAlignment="Right"
               TextAlignment="Right"
               Foreground="#374151"
               FontSize="14"/>
                        </Grid>

                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <Button Content="テスト読み上げ"
                                    Style="{StaticResource SubActionButtonStyle}"
                                    Command="{Binding TestSpeakCommand}"
                                    IsEnabled="{Binding IsVoiceEnabled}"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <!-- ボタン -->
                <StackPanel Grid.Row="5"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="OK"
                            Style="{StaticResource PrimaryButtonStyle}"
                            IsDefault="True"
                            Command="{Binding SaveCommand}"/>
                    <Button Content="キャンセル"
                            Style="{StaticResource SecondaryButtonStyle}"
                            IsCancel="True"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>